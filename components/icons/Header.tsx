import React from 'react';

// This file is a remnant from a previous refactor and is no longer used.
// It has been modified to prevent build errors.
const NeutralizedIconsHeader = () => null;
export default NeutralizedIconsHeader;
